@extends('layouts.app')
@section('title','بهترین قالیشویی‌های '.$location->name.' | قالی مپ')
@section('content')<div class="container section"><nav class="breadcrumbs"><a href="{{ route('home') }}">خانه</a><span>›</span><a href="{{ route('city.show',$city->slug) }}">{{ $city->name }}</a><span>›</span><span>بهترین قالیشویی‌های {{ $location->name }}</span></nav><div class="page-intro"><p class="eyebrow">{{ $city->name }} / {{ $location->type }}</p><h1>بهترین قالیشویی‌های {{ $location->name }}</h1><p>قالیشویی‌های فعال در {{ $location->name }}، {{ $city->name }}.</p></div><div class="cleaning-list">@forelse($location->carpetCleanings as $cleaning)<article class="cleaning-card"><div><h2>{{ $cleaning->name }}</h2><p>{{ $cleaning->description ?: ($cleaning->address ?: 'اطلاعات تکمیلی ثبت نشده است.') }}</p>@if($cleaning->address)<small>{{ $cleaning->address }}</small>@endif</div>@if($cleaning->phone)<a href="tel:{{ $cleaning->phone }}">{{ $cleaning->phone }}</a>@endif</article>@empty<div class="empty">هنوز قالیشویی برای این محدوده ثبت نشده است.</div>@endforelse</div></div>@endsection